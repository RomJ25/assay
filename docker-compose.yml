services:
  assay:
    build: .
    container_name: assay
    environment:
      - TZ=America/New_York
      - PYTHONUNBUFFERED=1
    volumes:
      - assay_data:/app/data
    restart: "no"

volumes:
  assay_data:
